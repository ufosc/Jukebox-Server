import type { Request, Response } from 'express'
import { responses } from 'src/utils'

export const createGroup = (_: Request, res: Response) => {
  return responses.notImplemented(res)
}

export const createGroupMember = (_: Request, res: Response) => {
  return responses.notImplemented(res)
}
export const createSessionGuest = (_: Request, res: Response) => {
  return responses.notImplemented(res)
}

export const getGroup = (_: Request, res: Response) => {
  return responses.notImplemented(res)
}

export const updateGroup = (_: Request, res: Response) => {
  return responses.notImplemented(res)
}

export const deleteGroup = (_: Request, res: Response) => {
  return responses.notImplemented(res)
}

export const getGroupMembers = (_: Request, res: Response) => {
  return responses.notImplemented(res)
}
