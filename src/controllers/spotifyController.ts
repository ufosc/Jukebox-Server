import { Request, Response } from "express"
import { responses } from "src/utils"

export const spotifyLogin = (req: Request, res: Response) => {
  return responses.notImplemented(res);
}

export const spotifyLoginCallback = (req: Request, res: Response) => {
  return responses.notImplemented(res)
}

export const spotifyTest = (req: Request, res: Response) => {
  return responses.notImplemented(res)
}

export const spotifySearch = (req: Request, res: Response) => {
  return responses.notImplemented(res)
}

export const spotifySearchTracks = (req: Request, res: Response) => {
  return responses.notImplemented(res)
}

export const spotifySearchTrackId = (req: Request, res: Response) => {
  return responses.notImplemented(res)
}

